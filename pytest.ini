[pytest]
testpaths = tests
pythonpath = .
markers =
    relay_docker: run with --relay-docker flag (builds and runs a local Docker container)
    relay_live: requires RELAY_URL env var pointing to a live relay server
